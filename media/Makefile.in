TARGET=Textures.xbt

all: $(TARGET)

.PHONY: force

$(TARGET):
ifeq (@ARCH@,arm)
	@TEXTUREPACKER@ -dupecheck -use_none -input . -output $(TARGET)
else
	@TEXTUREPACKER@ -dupecheck -input . -output $(TARGET)

endif

clean:
	rm -f $(TARGET)

distclean: clean

